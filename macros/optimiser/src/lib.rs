//! Optimises queries.
//!
//! Heavily inspired by the Relay compiler.
//!
//! Doesn't do much at the moment.

/// Optimise a query.
pub fn optimise(query: ()) -> () {}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
